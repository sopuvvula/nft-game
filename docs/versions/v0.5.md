# v0.5 — Visual Overhaul

> Goal: make the game look and feel like a real TCG (Hearthstone, MTG Arena, TFT) using only CSS/SVG — no images, no canvas, no heavy libraries.

---

## What Changed

### Foundation
- Google Fonts: **Cinzel** (fantasy serif for headings/card names) + **Inter** (clean sans for stats/UI)
- `src/ui/theme.ts` — single source of truth for card themes, keyword colors, board tokens, font constants
- CSS keyframe animations: `cardGlow`, `shimmer`, `summonFlash`, `energyPulse`, `tauntPulse`, `shieldShimmer`, `slideIn`, `fadeInOut`, `hpFlash`, `torchFlicker`, `fogDrift`, `runeFloat`, `emberFloat`, `starTwinkle`

### Hand Cards — Collectible Card Design
- Cards enlarged to ~110px wide with layered layout: cost gem, art panel, name banner, keyword badges, stat orbs
- **SVG character silhouettes** per template: armored knight (Vanguard), hooded assassin (Striker), channeling mage (Glass Cannon), towering guardian (Sentinel), ghostly wraith (Phantom), shield defender (Bulwark), charging lancer (Lancer)
- Cost gem: radial gradient with specular highlight
- Stat orbs: ATK (red, bottom-left) and HP (green, bottom-right) like Hearthstone
- Keyword badges: styled capsules with colored borders
- Fan layout: cards arc based on position (rotation + Y offset)
- Hover: lift (`translateY(-10px)`), scale, diagonal shimmer sweep
- Selected: pulsing glow animation

### Board Atmosphere
- Full arena background with stone pillars, arch, torches, fog, runes, and embers
- Player territories: blue-tinted rows for Player B, warm-tinted for Player A
- Center divider: glowing gradient line + "BATTLEFIELD" label in Cinzel
- Empty lanes: inset shadows (carved slot look)
- Active player territory: subtle inset gold glow

### Board Units — Mini Cards
- Mini SVG art (shared `CardArt` component at 30px)
- Template gradient background matching hand card theme
- Mini stat orbs (ATK/HP, 18px) at bottom
- Shield: shimmer animation overlay
- Spent: dimmed with red tint overlay
- Taunt target: pulsing amber border (CSS animation)

### Player HUD
- Circular avatar: crown SVG for active player, letter for inactive
- HP bar: 8px tall, gradient fill, dark border, HP number in Cinzel with glow
- Energy: diamond-shaped gems (rotated squares) with radial gradients + counter

### Controls & UI
- Buttons: Cinzel font, gradient fills, bottom-border depth trick (press effect), glow on hover
- Win screen: "VICTORY" text in Cinzel with gold glow/text-shadow
- Log: Inter font, left accent bar per message type (color-coded), slide-in animation
- Header: "TCG BLUEPRINT" with gold gradient text, styled turn/phase badges

### Arena Background
- Full SVG stone arena replacing simple gradient overlays
- 7-layer composited scene: sky with stars/moon, stone architecture (pillars + arch), perspective stone floor, animated torches with flicker, fog wisps with drift, floating arcane runes, ember particles
- All animations GPU-compositable (transform + opacity only)
- `ArenaBackground` component renders once, never re-renders (no props)

### Character Silhouettes (CardArt rewrite)
- Replaced geometric icons with recognizable character poses (20-28 SVG elements each)
- **Vanguard**: armored knight with shield held front, sword behind, visor slit glow
- **Striker**: hooded assassin lunging with dagger, billowing cloak, glowing eyes
- **Glass Cannon**: mage with arms raised channeling energy orb, pointed hat, lightning crackles
- **Sentinel**: towering guardian with massive shoulders, T-shaped visor glow, heavy plate
- **Phantom**: ghostly wraith floating with dissolving lower body, tattered hood, clawed hand
- **Bulwark**: defender crouched behind enormous tower shield, eyes peeking over top, rivets
- **Lancer**: charging warrior with extended lance, plumed helmet, trailing cape
- Characters remain distinguishable at 30px (board) — glow points stay visible, detail paths gracefully disappear

### Animations & Polish
- Turn transition banner: "PLAYER X'S TURN" fades in/out on turn change
- Arena torch flicker animation (opacity + scaleY pulse)
- Fog drift animation (slow horizontal translate)
- Floating rune bob animation (vertical + opacity pulse)
- Ember particles (upward drift with horizontal sway, amber/orange/gold colors)
- Star twinkle animation (subtle opacity pulse)
- Card shimmer: diagonal highlight sweep on hover

---

## Files Changed

| File | Action |
|------|--------|
| `index.html` | Added Google Fonts link |
| `src/styles.css` | CSS variables, keyframe animations (removed `particleFloat`) |
| `src/ui/theme.ts` | **New** — shared design tokens |
| `src/ui/ArenaBackground.tsx` | **New** — 7-layer arena scene (sky, pillars, floor, torches, fog, runes, embers) |
| `src/ui/ArenaBackground.css` | **New** — torch, fog, rune, ember, star keyframe animations |
| `src/ui/CardArt.tsx` | Full rewrite — 7 character silhouettes (20-28 elements each) |
| `src/ui/Hand.tsx` | Major rewrite — collectible card design |
| `src/ui/Hand.css` | **New** — hover, glow, shimmer animations |
| `src/ui/Board.tsx` | Major rewrite — atmosphere, mini-card units, HUD |
| `src/ui/Board.css` | **New** — unit animations, hover states |
| `src/ui/Controls.tsx` | Button redesign, win screen |
| `src/ui/Controls.css` | **New** — button hover/active states |
| `src/ui/Log.tsx` | Typography, accent bars |
| `src/ui/Log.css` | **New** — slide-in animation |
| `src/App.tsx` | Replaced old atmosphere divs + Particle with ArenaBackground |

---

## Constraints
- **No engine changes** — all 45 smoke tests pass unmodified
- CSS/SVG only — no images, canvas, or external UI libraries
- No new game mechanics or card types
- Designed for 1280px+ screens

---

## Out of Scope
- Sound effects
- Card play/summon animations (beyond CSS transitions)
- Mobile/responsive layout
- Card back design
- Drag-and-drop interactions
