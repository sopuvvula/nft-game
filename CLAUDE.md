# Project Rules

## Documentation Requirements

When completing a versioned feature (v0.X):
1. **Create** `docs/versions/v0.X.md` with goal, changes, files changed, constraints, and out-of-scope sections
2. **Update** `context.md` version table with the new row
3. Documentation must be included in the **same commit** as the code changes — never ship code without updating docs
